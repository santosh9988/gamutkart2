version: 2.0
jobs:
  build:
    docker:
      - image: santosh9988/nginx
    steps:
      - checkout
      - run: sudo apt-get update -y
      - run: sudo apt-get install git -y
      - run: git --version
